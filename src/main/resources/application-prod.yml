server:
  address: 127.0.0.1
  error:
    include-stacktrace: never
    include-message: never
    include-binding-errors: never

logging:
  level:
    root: INFO

security:
  cors:
    allowed-origins: "https://www.dmeverly.com,https://dmeverly.com"